import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'www.genshinwikimpact.com',
  appName: 'Genshin Wikimpact',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
